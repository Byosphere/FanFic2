<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:focusable="true"
    android:clickable="true"
    android:background="?android:attr/selectableItemBackground"
    android:id="@+id/row_writings">

    <ImageView
        android:id="@+id/avatar_writings"
        android:layout_width="50dp"
        android:layout_height="50dp"
        tools:background="#AAA"
        android:background="#AAA"
        android:layout_marginTop="5dp"
        />

    <TextView
        android:id="@+id/titre_writings"
        android:layout_width="150dp"
        android:layout_height="wrap_content"
        android:textColor="@android:color/black"
        android:layout_marginLeft="10dp"
        android:textSize="8pt"
        tools:text="Titre"
        android:layout_alignParentTop="true"
        android:layout_toEndOf="@+id/avatar_writings"
        android:maxLines="1"
        android:ellipsize="end"/>

    <ImageButton
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:id="@+id/write_new_page"
        android:background="@drawable/ic_write"
        android:layout_alignBottom="@+id/avatar_writings"
        android:layout_alignParentEnd="true" />

    <ImageButton
        android:layout_width="40dp"
        android:layout_height="40dp"
        android:id="@+id/imageButton"
        android:background="@drawable/ic_lock_open"
        android:layout_alignTop="@+id/write_new_page"
        android:layout_toStartOf="@+id/write_new_page"
        android:layout_marginEnd="10dp" />

</RelativeLayout>