<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="10dp"
    android:focusable="true"
    android:clickable="true"
    android:background="?android:attr/selectableItemBackground"
    android:id="@+id/row_writings">


    <ImageButton
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_margin="10dp"

        android:id="@+id/write_new_page"
        android:background="@drawable/ic_write"
        android:layout_alignParentStart="true" />

    <TextView
        android:id="@+id/titre_writings"
        android:layout_width="220dp"
        android:layout_height="wrap_content"
        android:textColor="@android:color/black"
        android:layout_marginStart="10dp"
        android:layout_alignBottom="@+id/write_new_page"
        android:textSize="8pt"
        tools:text="Titre"
        android:maxLines="1"
        android:ellipsize="end"
        android:layout_toEndOf="@+id/write_new_page" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        tools:text="12 pages"
        android:id="@+id/pages_write"
        android:layout_alignTop="@+id/titre_writings"
        android:layout_alignParentEnd="true" />


</RelativeLayout>